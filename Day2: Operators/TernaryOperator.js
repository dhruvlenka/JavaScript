var age = 18;
console.log(age >= 18 ? "Can Vote" : "Minor");

let score = 264;
let grade = score >= 150 ? "A" : score >= 100 ? "B" : "C";
console.log(grade);
console.log(score >= 200 ? "A" : score >= "100" ? "B" : "C");
